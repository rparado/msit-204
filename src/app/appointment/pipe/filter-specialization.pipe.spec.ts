import { FilterSpecializationPipe } from './filter-specialization.pipe';

describe('FilterSpecializationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpecializationPipe();
    expect(pipe).toBeTruthy();
  });
});
